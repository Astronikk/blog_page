---
const { post } = Astro.props;
---
<article class="border border-gray-200 dark:border-gray-700 rounded-lg p-4 hover:shadow-lg transition-shadow duration-200">
  <h3 class="text-lg font-semibold mb-1"><a href={post.url}>{post.frontmatter.title}</a></h3>
  <p class="text-sm text-gray-600 dark:text-gray-400">{post.frontmatter.description}</p>
  <time class="text-xs text-gray-500">{post.frontmatter.pubDate}</time>
</article>
